1. 判断公交车班次方向（根据起点站和终点站的500米内运行轨迹）
    1. 0方向班次
    2. 1方向班次
    3. 识别环形公交班次
    ===> 得到每个班次的开始时间和结束时间以及方向
2. 各个班次的具体运行信息
    ===> 公交轨迹+到达站点顺序号
    ===> 每个班次到达各个站点的时间（最早时间和最晚时间），并修正始发时间
3. 根据经纬度合并gps轨迹
4. 根据公交轨迹两个点之间的距离构建时空格
5. 计算各条线路最大最小经纬度，以缩小后期匹配用户轨迹的范围

-- 公交车起点到终点为下行，终点到起点为上行
